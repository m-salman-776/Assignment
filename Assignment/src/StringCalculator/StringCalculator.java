package StringCalculator;

public class StringCalculator {
	public  int Add(String numbers) {
		int sum = 0;
		int string_length = numbers.length();
		if(string_length == 0) return sum;
		return sum;
	}
}
